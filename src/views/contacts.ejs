<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <%- include("components/links/loader-links") %> <%-
        include("components/links/nav-links") %> <%-
        include("components/links/contacts-links") %>
        <title>Contacts</title>
    </head>
    <body>
        <div class="main-page">
            <%- include("components/parts/loader") %> <%-
            include("components/parts/nav") %>
            <h1>Logged in as <%=name %></h1>
        </div>
    </body>
</html>
