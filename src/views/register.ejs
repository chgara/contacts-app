<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <%- include("components/links/loader-links") %> <%-
        include("components/links/login-register") %>
        <link rel="stylesheet" href="/css/register.css" />
        <title>Register</title>
    </head>
    <body>
        <%- include("components/parts/loader") %>
        <div class="loging-page">
            <div class="loging-box">
                <h3 class="loging-title">Please Register</h3>
                <form method="POST" action="/register">
                    <div class="input-group">
                        <label for="userName">User Name</label>
                        <input
                            type="text"
                            id="username"
                            name="username"
                            placeholder="Select a username"
                            value="<%= typeof username!= 'undefined' ? username : '' %>"
                        />
                    </div>
                    <div class="input-group">
                        <label for="useremail">Email</label>
                        <input
                            type="email"
                            id="email"
                            name="useremail"
                            placeholder="Put your Email"
                            value="<%= typeof email !=
                        'undefined' ? email : null %> "
                        />
                    </div>
                    <div class="input-group">
                        <label for="password">Password</label>
                        <input
                            type="password"
                            name="password"
                            id="password"
                            placeholder="Put your Password"
                            value="<%= typeof
                        password!= 'undefined' ? password : '' %>"
                        />
                    </div>
                    <div class="input-group">
                        <label for="confirm-password">Confirm Password</label>
                        <input
                            type="password"
                            name="password2"
                            id="confirm-password"
                            placeholder="Put the password"
                            value="<%= typeof password2!= 'undefined' ? password2 :
                        '' %>"
                        />
                    </div>
                    <div class="loging-box-button">
                        <button
                            type="submit"
                            class="loging-button"
                            value="Register"
                        >
                            Sing In
                        </button>
                    </div>
                    <div class="register-box-txt">
                        <p class="register-txt">
                            You have an accoutn?
                            <a href="/login">Log in now</a>
                            or go
                            <a href="/home">home</a>
                        </p>
                    </div>
                </form>
                <%- include("components/parts/error-success") %>
            </div>
        </div>
    </body>
</html>
